# LaTeX intermediate files
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
_ul
nul
cited_keys.txt

# Dynare intermediate files
+vietnam_dsge/
vietnam_dsge/
vietnam_dsge.log
+vietnam_dsge_proptax/
vietnam_dsge_proptax/
vietnam_dsge_proptax.log

# MATLAB
*.asv
*.m~
*.mat
*.csv

# However, you might want to keep the final dynare results and IRFs around, so we force-add them later or keep them out of gitignore if needed. Actually, let's keep `.mat` and `.csv` in gitignore to prevent huge diffs, but we will track your main source files.

# Wickens variant (work in progress, not yet published)
*wickens*
Wickens_*
proptax_wickens_results.json
dsge_model_vietnam_wickens.py

# LaTeX editor temp files
*.tex.tmp.*

# Log/output capture files
latex_err.txt
latex_out.txt
matlab_err.txt
matlab_error.txt
matlab_out.txt
matlab_output.txt

# Misc
.DS_Store
.claude/
zixU9qGI
"Laptop K1 - Shortcut.lnk"
__pycache__/
*.nav
*.snm
